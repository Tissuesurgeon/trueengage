services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: trueengage
      POSTGRES_PASSWORD: trueengage
      POSTGRES_DB: trueengage
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
